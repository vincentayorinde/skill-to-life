# scoring

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build scoring` to build the library.

## Running unit tests

Run `nx test scoring` to execute the unit tests via [Vitest](https://vitest.dev/).
